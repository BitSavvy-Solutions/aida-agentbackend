name: Trigger auto deployment for aida-agentbackend-prod

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/aida-agentbackend-prod-AutoDeployTrigger-08090d31-50fe-4ccd-a698-1591956e838a.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AIDAAGENTBACKENDPROD_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AIDAAGENTBACKENDPROD_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AIDAAGENTBACKENDPROD_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          _targetLabelKey_: _targetLabel_
          registryUrl: iverseregistry.azurecr.io
          registryUsername: ${{ secrets.AIDAAGENTBACKENDPROD_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.AIDAAGENTBACKENDPROD_REGISTRY_PASSWORD }}
          containerAppName: aida-agentbackend-prod
          resourceGroup: iverse
          imageToBuild: iverseregistry.azurecr.io/aida-agentbackend-prod:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


